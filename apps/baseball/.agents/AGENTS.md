# AGENTS.md (legacy location)

The canonical instructions for this repository live in `AGENTS.md` at the repository root (one directory above this file).

Component-library-specific rules live in `web-components/.agents/AGENTS.md` (relative to the repository root).
